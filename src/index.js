import React from "react";
import ReactDOM from "react-dom";

import "./styles.css";

import Layout from "./components/API";

const rootElement = document.getElementById("root");
ReactDOM.render(<API />, rootElement);
